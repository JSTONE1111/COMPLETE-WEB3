# DO NOT commit this file with real values!
# This is a template - create secrets using kubectl commands instead

---
apiVersion: v1
kind: Secret
metadata:
  name: github-sheets-secrets
  namespace: default
type: Opaque
stringData:
  WEBHOOK_SECRET: "your-github-webhook-secret"
  SHEET_ID: "your-google-sheet-id"
  CLIENT_ID: "your-google-client-id"
  CLIENT_SECRET: "your-google-client-secret"
  REDIRECT_URI: "your-redirect-uri"

---
apiVersion: v1
kind: Secret
metadata:
  name: google-credentials
  namespace: default
type: Opaque
data:
  # Base64 encoded credentials.json
  # Create with: cat credentials.json | base64 -w 0
  credentials.json: ""